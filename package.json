{
  "name": "city-builder-fullstack",
  "version": "1.0.0",
  "description": "City Traffic Builder - Fullstack App",
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && python manage.py runserver",
    "frontend": "cd frontend && npm run dev",
    "install-all": "npm install && cd frontend && npm install",
    "build": "cd frontend && npm run build",
    "setup": "npm run install-all && cd backend && python -m venv venv && venv\\Scripts\\activate && pip install -r requirements.txt && python manage.py makemigrations && python manage.py migrate"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}